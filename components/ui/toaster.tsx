'use client';

// This component is a stub — the app uses Sonner (components/ui/sonner.tsx) for all toasts.
// Kept here to avoid import errors from any auto-generated shadcn code.
export function Toaster() {
  return null;
}
